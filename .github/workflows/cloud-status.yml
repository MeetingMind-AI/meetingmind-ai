name: "🔎 Cloud — Status"

# Manual button: is the GPU box currently up (and billing) or not?
on:
  workflow_dispatch:

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Scaleway CLI + jq
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq jq
          gh release download --repo scaleway/scaleway-cli --pattern '*_linux_amd64' --output ./scw --clobber
          sudo install -m 0755 ./scw /usr/local/bin/scw
          scw version
      - name: Report status
        env:
          SCW_ACCESS_KEY: ${{ secrets.SCW_ACCESS_KEY }}
          SCW_SECRET_KEY: ${{ secrets.SCW_SECRET_KEY }}
          SCW_DEFAULT_PROJECT_ID: ${{ secrets.SCW_DEFAULT_PROJECT_ID }}
          SCW_DEFAULT_ORGANIZATION_ID: ${{ secrets.SCW_DEFAULT_ORGANIZATION_ID }}
        run: bash deploy/scaleway/cloud.sh status | tee -a "$GITHUB_STEP_SUMMARY"
